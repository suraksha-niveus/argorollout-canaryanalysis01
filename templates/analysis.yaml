# kind: AnalysisTemplate
# apiVersion: argoproj.io/v1alpha1
# metadata:
#   name: canarycheck
# spec:
#   metrics:
#   - name: canarycheck
#     failureLimit: 1
#     interval: 1
#     successCondition: result >= "0.95"
#     # successCondition: result.successRate >= 0.95
#     provider:
#       web:
#         # paceholders are resolved when an AnalysisRun is created
#         url: "http://34.93.157.58:5000/"
#         jsonPath: "{$.status}"
#         # query: FROM Transaction SELECT percentage(count(*), WHERE httpResponseCode != 500) as successRate where appName = "http://34.93.157.58:5000/"

   
        
        

# kind: AnalysisTemplate
# apiVersion: argoproj.io/v1alpha1
# metadata:
#   name: canarycheck
# spec:
#   metrics:
#   - name: success-rate
#     successCondition: result.successRate >= 0.95
#     provider:
#       newRelic:
#         profile: argo-rollouts-newrelic-nonprod-credentials
#         query: FROM Transaction SELECT percentage(count(*), WHERE httpResponseCode != 500) as successRate where appName = "http://34.93.157.58:5000/"


kind: AnalysisTemplate
apiVersion: argoproj.io/v1alpha1
metadata:
  name: canarycheck
spec:
  metrics:
  - name: canarycheck
    # failureLimit: 1
    # interval: 1
    # successCondition: result == "ok"
    provider:
      web:
        # paceholders are resolved when an AnalysisRun is created
        url: "http://34.93.157.58:5000/"
        jsonPath: "{$.status}"


# kind: AnalysisTemplate
# apiVersion: argoproj.io/v1alpha1
# metadata:
#   name: canarycheck
# spec:
#   metrics:
#   - name: test
#     provider:
#       job:
#         spec:
#           backoffLimit: 1
#           template:
#             spec:
#               containers:
#               - name: busybox
#                 image: busybox
#                 #args: [test]  #--> for making analysis fail, uncomment
#               restartPolicy: Never

